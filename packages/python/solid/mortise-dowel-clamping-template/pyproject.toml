[project]
name = "mortise-dowel-clamping-template"
version = "0.1.0"
description = "Mortise Dowel Clamping Template generator"
requires-python = ">=3.12"
dependencies = [
    "solidpython2>=2.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
